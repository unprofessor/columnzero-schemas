[project]
name = "columnzero-schemas"
version = "0.1.0"
description = "Immutable JSON Schema publisher for schemas.columnzero.com"
requires-python = ">=3.11"
dependencies = ["jsonschema>=4.23,<5"]

[tool.pytest.ini_options]
testpaths = ["tests"]
